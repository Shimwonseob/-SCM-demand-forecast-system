import streamlit as st

st.set_page_config(page_title="시스템설명", layout="wide", page_icon="📖")
st.title("📖 시스템 사용 가이드")

# ── 섹션 1: 시스템 개요 ───────────────────────────────────────────────────────
st.subheader("🔎 시스템 개요")
st.markdown("""
본 시스템은 관세청 전자상거래 무역통계 데이터를 기반으로 **수입·수출 금액을 예측**하고,
다양한 시나리오를 시뮬레이션할 수 있는 수요예측 대시보드입니다.
통계 모델(SARIMA)·머신러닝(XGBoost)·시계열 분해(Prophet)·앙상블을 결합하여
단일 모델 대비 안정적인 예측을 제공합니다.
""")

col1, col2, col3 = st.columns(3)
with col1:
    st.info("**데이터 출처**\n\n관세청 전자상거래무역통계\n(2014.01 ~ 2026.02)")
with col2:
    st.info("**예측 기간**\n\n2026년 3월 ~ 12월\n(10개월 선행 예측)")
with col3:
    st.info("**사용 모델**\n\nSARIMA / Prophet\nXGBoost / 앙상블")

st.divider()

# ── 섹션 2: 페이지별 사용 방법 ───────────────────────────────────────────────
st.subheader("📄 페이지별 사용 방법")
st.markdown("""
| 페이지 | 목적 | 주요 기능 | 권장 사용 순서 |
|--------|------|-----------|---------------|
| 📊 대시보드 | 전체 현황 파악 | 수입·수출 요약 카드, 추천 모델, 미니 차트, 핵심 인사이트 | 1 — 처음 접속 시 |
| 🔍 데이터탐색 | 패턴·계절성 분석 | 시계열 개요, 계절 분해, 히트맵, 코로나 전후 비교, 무역수지 | 2 — 데이터 이해 |
| 📈 수요예측 | 4개 모델 예측 실행 | 멀티모델 예측 차트, 신뢰구간, 백테스트 성능, 예측값 테이블·다운로드 | 3 — 핵심 기능 |
| 🔀 시나리오분석 | 낙관·비관·충격 시뮬레이션 | 시나리오 비교 차트, 연간 합계 바차트, 상세 테이블 | 4 — 전략 수립 |
| 🧠 모델분析 | 백테스트 및 모델 신뢰도 확인 | 예측 vs 실제 차트, 오차 분석, 성능 리포트, 모델 선택 가이드 | 5 — 모델 검토 |
| ✅ 예측검증 | 실제값 입력 후 오차 확인 | 오차율 계산, 정확도 판정, 후속조치 가이드 | 6 — 사후 검증 |
""")

st.divider()

# ── 섹션 3: 파라미터 조작 가이드 ─────────────────────────────────────────────
st.subheader("⚙️ 파라미터 조작 가이드")

with st.expander("📅 학습 시작 연도 슬라이더 (수요예측 페이지)", expanded=True):
    st.markdown("""
| 설정값 | 효과 | 권장 상황 |
|--------|------|-----------|
| **2014 ~ 2017** | 장기 트렌드 반영, COVID 충격 포함 | 장기 패턴 중심 예측 필요 시 |
| **2018 ~ 2020** | 기본값. 균형적인 학습 데이터 | 일반적인 사용 (기본 권장) |
| **2021 ~ 2022** | 최근 데이터 중심, 빠른 변화 반영 | 최근 트렌드가 급변했을 때 |

- 학습 데이터가 **24개월 미만**이면 XGBoost 정확도가 떨어지므로 경고 표시됩니다.
- COVID 충격(2020년) 이전 데이터를 제외하면 모델이 충격 없는 성장 패턴을 학습합니다.
""")

with st.expander("⚖️ 앙상블 가중치 슬라이더 (수요예측 페이지)"):
    st.markdown("""
세 모델(SARIMA / Prophet / XGBoost) 예측값의 가중 평균이 앙상블 예측값입니다.
가중치 합이 1이 되도록 내부에서 자동 정규화됩니다.

| 상황 | 권장 가중치 조정 |
|------|-----------------|
| 계절성이 강하고 데이터가 안정적 | SARIMA 비중 ↑ (0.5 이상) |
| 최근 2~3년간 추세 변화가 뚜렷 | Prophet 비중 ↑ (changepoint 많을 때) |
| 비선형 패턴·외부 요인 반영 필요 | XGBoost 비중 ↑ |
| 모델 간 MAPE 차이가 적음 | 균등 가중치(0.4 / 0.4 / 0.2) 유지 |

모든 슬라이더를 0으로 설정하면 자동으로 균등 가중치(1/3씩)가 적용됩니다.
""")

with st.expander("📉 낙관·비관 승수 (시나리오분석 페이지)"):
    st.markdown("""
기본 예측값에 승수를 곱해 낙관·비관 시나리오를 생성합니다.

| 파라미터 | 범위 | 의미 |
|----------|------|------|
| 낙관 승수 | 1.05 ~ 1.30 | 기본 대비 +5% ~ +30% 성장 가정 |
| 비관 승수 | 0.70 ~ 0.99 | 기본 대비 -1% ~ -30% 감소 가정 |

**예시:** 낙관 1.15 설정 시 → 기본 예측 $100M → 낙관 $115M으로 표시
""")

with st.expander("⚡ 충격 시뮬레이션 (시나리오분석 페이지)"):
    st.markdown("""
특정 월에 외부 충격(정책·관세·이슈)을 임의로 적용해 영향을 시뮬레이션합니다.

1. **충격 적용 토글** ON → 충격 설정 슬라이더 표시
2. **충격 발생 월** 선택: 2026-03 ~ 2026-12 중 선택
3. **충격 강도 (%)** 설정: 음수(-) = 감소, 양수(+) = 증가
4. **충격 지속 기간** 설정: 1 ~ 6개월

**예시:** 2026-06에 -20% 충격 2개월 → 6·7월 예측값이 기본 대비 80%로 감소
""")

st.divider()

# ── 섹션 4: 모델 설명 ─────────────────────────────────────────────────────────
st.subheader("🧠 모델 설명")

m1, m2 = st.columns(2)
m3, m4 = st.columns(2)

with m1:
    with st.container(border=True):
        st.markdown("**🔵 SARIMA** (Seasonal ARIMA)")
        st.markdown(
            "계절성 자기회귀 이동평균 모델. 과거 값의 선형 패턴과 계절 주기를 통계적으로 모델링합니다. "
            "파라미터(p,d,q)가 명시적이어서 해석이 용이하고, 안정적인 시계열에 강점이 있습니다."
        )

with m2:
    with st.container(border=True):
        st.markdown("**🟢 Prophet** (Meta)")
        st.markdown(
            "추세·계절성·휴일 효과를 자동으로 분해하는 베이지안 시계열 모델. "
            "변화점(changepoint)을 자동 감지하여 급격한 트렌드 전환 구간에서 높은 유연성을 보입니다."
        )

with m3:
    with st.container(border=True):
        st.markdown("**🟠 XGBoost** (Gradient Boosting)")
        st.markdown(
            "lag·rolling 통계 피처 기반의 트리 앙상블 모델. 비선형 패턴 학습이 강력하며, "
            "월·연도 정보 및 과거 12개월 이동평균을 피처로 활용합니다."
        )

with m4:
    with st.container(border=True):
        st.markdown("**🔴 앙상블**")
        st.markdown(
            "SARIMA·Prophet·XGBoost 세 모델의 가중 평균(기본 40%·40%·20%). "
            "단일 모델의 과적합 리스크를 분산하여 전반적으로 안정적인 예측을 제공합니다."
        )

st.divider()

# ── 섹션 5: 기술 스택 ─────────────────────────────────────────────────────────
st.subheader("🛠️ 기술 스택")
st.markdown("""
| 라이브러리 | 버전 기준 | 용도 |
|------------|-----------|------|
| **Streamlit** | 1.57+ | 웹 앱 프레임워크 |
| **Pandas** | 2.x | 데이터 처리 및 시계열 인덱싱 |
| **NumPy** | 1.x | 수치 연산 |
| **Plotly** | 5.x | 인터랙티브 시각화 |
| **pmdarima** | 2.x | Auto-ARIMA (SARIMA 자동 파라미터 선택) |
| **Prophet** | 1.1+ | Facebook 시계열 모델 |
| **XGBoost** | 2.x | 그래디언트 부스팅 트리 |
| **statsmodels** | 0.14+ | 계절성 분해 (STL) |
| **openpyxl** | 3.x | 엑셀 파일 읽기·쓰기 |
| **kaleido** | 0.2+ | Plotly 차트 PNG 내보내기 |
""")

st.caption("데이터 출처: 관세청 전자상거래 무역통계 (bandtrass.or.kr)  |  분석 기준일: 2026년 5월  |  버전: v1.1.0")
